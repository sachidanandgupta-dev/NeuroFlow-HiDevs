[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neuroflow-sdk"
version = "0.1.0"
description = "Official Python SDK for NeuroFlow AI Platform"
dependencies = ["httpx>=0.27.0"]